# --- Web framework ---
fastapi==0.110.0
uvicorn[standard]==0.27.1
python-multipart==0.0.9
itsdangerous==2.1.2

# --- DB / ORM / migrations ---
sqlalchemy[asyncio]==2.0.27
asyncpg==0.29.0
alembic==1.13.1

# --- Settings / validation ---
pydantic==2.6.1
pydantic-settings==2.2.1
email-validator==2.1.0  # for Pydantic EmailStr

# --- Auth ---
python-jose[cryptography]==3.3.0
authlib==1.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt <4.1: passlib 1.7.4 imports bcrypt.__about__ for version
# detection, which was removed in bcrypt 4.1+. Without this pin, password
# hashing crashes with "password cannot be longer than 72 bytes" on every
# call. When passlib ships a release compatible with bcrypt 5, drop the pin.
bcrypt==4.0.1

# --- Resume storage ---
cloudinary==1.39.1

# --- HTTP client (OAuth userinfo, Cloudinary fallback) ---
httpx==0.27.0
